#include <stdio.h>

void	quicksort(int *arr, unsigned int size);

int	main(void)
{
	
	return (0);
}

void	quicksort(int *arr, unsigned int size)
{
	int		pivot = (arr[0] + arr[size - 1]) / 2;
	int		*left = arr, *right = arr + size - 1;

	while (left < right)
	{
		
	}
}
