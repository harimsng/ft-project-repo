#include <stdio.h>
#include <string.h>

unsigned int	ft_strlcat(char *dest, char *src, unsigned int size);

int	main(void)
{
	char	dest1[10];
	char	dest2[10];
	char	*src;
	int		idx;

	idx = 0;
	while (idx < 9)
	{
		dest1
	dest = 0;
	src = 0;

